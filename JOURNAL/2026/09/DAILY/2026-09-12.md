# 2026-09-12

## Today's Focus: AWS Project

## Network Architecture

```mermaid
flowchart TB
  USER["Internet (Allowed CIDRs)"]
  DNS["Route 53"]
  AWS["AWS Public Endpoints：ECR, S3, Secrets Manager, CloudWatch"]

  subgraph VPC["VPC：10.42.0.0/16"]
    IGW["Internet Gateway"]

    subgraph PUBLIC["Public Subnet (public[0] AZ-A ＋ public[1] AZ-B)"]
      PALB["Public ALB (Across Two Subnets, 443)"]
      NAT0["NAT 0 ＋ EIP 0：Located at public[0]"]
      NAT1["NAT 1 ＋ EIP 1：Located at public[1]"]
    end

    RTP["Public Route Table (Shared by Two Public Subnets)"]

    subgraph PRIVATE["Private Subnet (private[0] AZ-A ＋ private[1] AZ-B)"]
      FE["Frontend Service (8080)"]
      IALB["Internal ALB (Across Two Subnets, 8081)"]
      BE["Backend Service (8081)"]

      subgraph APP1["App1"]
        AG1["Agent sidecar (8090)"]
        AP1["Application (8082)"]
      end

      subgraph APP2["App2"]
        AG2["Agent sidecar (8090)"]
        AP2["Application (8082)"]
      end

      INIT["init-db ECS Task (One Time)"]
    end

    RT0["private[0] Route Table"]
    RT1["private[1] Route Table"]

    subgraph DATABASE["Database Subnet (database[0] AZ-A ＋ database[1] AZ-B)"]
      DB[("Oracle (Primary,1521)")]
      STANDBY[("Oracle (Standby, Another AZ)")]
    end

    RTD["Database Route Table (Shared by Two DB Subnets, Only Local)"]
  end

  USER -.->|"DNS Query"| DNS
  DNS -.->|"Alias Points"| PALB
  USER -->|"HTTPS 443"| IGW
  IGW -->|"Web Entry"| PALB

  PALB -->|"HTTP 8080"| FE
  FE -->|"HTTP 8081"| IALB
  IALB -->|"HTTP 8081"| BE

  BE -->|"Service Connect (8090)"| AG1
  BE -->|"Service Connect (8090)"| AG2
  AG1 -->|"127.0.0.1:8082"| AP1
  AG2 -->|"127.0.0.1:8082"| AP2

  AG1 -->|"Confirm Job Claim (8081)"| IALB
  AG2 -->|"Confirm Job Claim (8081)"| IALB

  FE -->|"Read Data (1521)"| DB
  BE -->|"Read/Write Data (1521)"| DB
  INIT -->|"Create DB Structure (1521)"| DB
  DB -->|"Sync Replication"| STANDBY

  PUBLIC -.->|"Associate Two Subnets"| RTP
  RTP -.->|"0.0.0.0/0 → IGW"| IGW

  PRIVATE -.->|"private[0] Associate"| RT0
  PRIVATE -.->|"private[1] Associate"| RT1
  RT0 -.->|"0.0.0.0/0 → NAT 0"| NAT0
  RT1 -.->|"0.0.0.0/0 → NAT 1"| NAT1

  DATABASE -.->|"Associate Two Subnets"| RTD
  NAT0 -->|"Outbound 443"| IGW
  NAT1 -->|"Outbound 443"| IGW
  IGW -->|"AWS Public API 443"| AWS

  style PUBLIC fill:#dff7e6,stroke:#166534,color:#111827
  style PRIVATE fill:#dbeafe,stroke:#1d4ed8,color:#111827
  style DATABASE fill:#ffedd5,stroke:#c2410c,color:#111827
  style APP1 fill:#f8fafc,stroke:#334155,color:#111827
  style APP2 fill:#f8fafc,stroke:#334155,color:#111827
  classDef route fill:#ede9fe,stroke:#6d28d9,color:#111827
  class RTP,RT0,RT1,RTD route

```

## Mapping Between Subnets and Routes


| Terraform Subnet         | AZ   | CIDR            | Route Table                  |
|--------------------------|------|-----------------|------------------------------|
| `aws_subnet.public[0]`   | AZ-A | `10.42.0.0/24`  | `aws_route_table.public`     |
| `aws_subnet.public[1]`   | AZ-B | `10.42.1.0/24`  | `aws_route_table.public`     |
| `aws_subnet.private[0]`  | AZ-A | `10.42.10.0/24` | `aws_route_table.private[0]` |
| `aws_subnet.private[1]`  | AZ-B | `10.42.11.0/24` | `aws_route_table.private[1]` |
| `aws_subnet.database[0]` | AZ-A | `10.42.20.0/24` | `aws_route_table.database`   |
| `aws_subnet.database[1]` | AZ-B | `10.42.21.0/24` | `aws_route_table.database`   |


| Route Table  | VPC Local Route        | Default Route                  |
|--------------|------------------------|--------------------------------|
| `public`     | `10.42.0.0/16 → local` | `0.0.0.0/0 → Internet Gateway` |
| `private[0]` | `10.42.0.0/16 → local` | `0.0.0.0/0 → NAT 0`            |
| `private[1]` | `10.42.0.0/16 → local` | `0.0.0.0/0 → NAT 1`            |
| `database`   | `10.42.0.0/16 → local` | No Default Route               |

