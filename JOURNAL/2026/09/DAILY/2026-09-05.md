# 2026-09-05

## Today's Focus: AWS Migration

## Existing Architecture

- Frontend Services, Backend Services and Agents are all Java-Based program and are deployed using 
Podman on VMs. 
- For high availability, the Frontend Services and Backend Services are deployed in an active-active configuration across
  different VMs in different data centers.
- Oracle is used as the database and is deployed in an HA configuration  across different data centers. 


```mermaid
flowchart TB
   
    subgraph Core["Core Components"]
        
        
        subgraph Frontend["Frontend Services"]
            direction LR
            Frontend1["Frontend Service (VM1)"]
            Frontend2["Frontend Service (VM2)"]
        end

        subgraph Backend["Backend Services"]
          direction LR
            Backend1["Backend Service (VM3)"]
            Backend2["Backend Service (VM4)"]
        end
        
        Database[(Database-Oracle)]
    end
    
    subgraph Apps["Apps"]

        subgraph App_1["App 1"]
            Agent1["Agent"]
            App1_service["App 1 Service"]
        end

      subgraph App_2["App 2"]
            Agent2["Agent"]
            App2_service["App 2 Service"]
      end
    end
    
    User --> DNS 
    DNS --> LB1["LB (F5/A10)"]
    LB1 --> Frontend
    Frontend --> LB2["LB (F5/A10)"]
    LB2 --> Backend
    Frontend --> Database
    Backend --> Database
    Backend -- TCP/REST --> Agent1
    Backend -- TCP/REST --> Agent2
```

## Migrating to AWS Architecture

- Frontend Services and Backend Services are deployed as active-active ECS services, 
with multiple Fargate Tasks distributed across different AZs. 
- RDS is used as the database with Multi-AZ deployment for high availability. 
- Each Agent and its corresponding Application are deployed in the same ECS Fargate Task, 
  with the Agent running as a sidecar container.


```mermaid
flowchart TB
   
    subgraph Core["Core Components"]
        
        
        subgraph Frontend["Frontend Services (ECS Service)"]
            direction LR
            Frontend1["Frontend Service (ECS Task_frontend1 AZ-a)"]
            Frontend2["Frontend Service (ECS Task_frontend2 AZ-b)"]
        end

        subgraph Backend["Backend Services (ECS Service)"]
          direction LR
            Backend1["Backend Service (ECS Task_backend1 AZ-a)"]
            Backend2["Backend Service (ECS Task4_backend2 AZ-b)"]
        end
        
        Database[(Database-RDS-Multi-AZ)]
    end
    
    subgraph Apps["Apps"]

        subgraph App_1["App 1 (ECS Service)"]
            Agent1["Agent"]
            App1_service["App 1 Service"]
        end

      subgraph App_2["App 2 (ECS Service)"]
            Agent2["Agent"]
            App2_service["App 2 Service"]
      end
    end
    
    User --> Route53
    Route53 --> LB1["ALB"]
    LB1 --> Frontend
    Frontend --> LB2["ALB"]
    LB2 --> Backend
    Frontend --> Database
    Backend --> Database
    Backend -- TCP/REST --> Agent1
    Backend -- TCP/REST --> Agent2
```

## Migration Consideration

- VM + Podman -> ECS Fargate
- F5/A10 -> ALB
- Oracle -> RDS
- Database HA -> RDS Muti-AZ
- Active-active VMs -> Multi-AZ ECS FargateTasks
- Agent containers -> Sidecar containers

